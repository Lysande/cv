<div class="education panel">
	<table>
		<thead>
			<tr>
				<td>Utbildning</td>
				<td>Skola</td>
				<td>Från</td>
				<td>Till</td>
			</tr>
		</thead>
		<tbody>
			{{#each this}}
			    <tr>
			      <td>{{this.name}}</td>
			      <td>{{this.school}}</td>
			      <td>{{this.from}}</td>
			      <td> {{this.to}}</td>
			    </tr>
			{{/each}}
		</tbody>
	</table>
</div>
